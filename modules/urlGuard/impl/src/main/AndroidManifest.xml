<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <application>
        <activity
            android:name="com.safeNest.demo.features.urlGuard.impl.presentation.UrlGuardActivity"
            android:exported="false"
            android:label="@string/title_activity_url_guard" />

        <activity
            android:name="com.safeNest.demo.features.urlGuard.impl.presentation.AudioRecordTest"
            android:exported="false" />
        <!--
            Transparent trampoline that shows the "Start recording?" system dialog
            and relays the MediaProjection token back to UrlGuardAccessibilityService.
            Must be excluded from Recents so it leaves no task entry.
        -->
        <activity
            android:name="com.safeNest.demo.features.urlGuard.impl.urlGuard.ScreenCapturePermissionActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <service
            android:name="com.safeNest.demo.features.urlGuard.impl.urlGuard.UrlGuardAccessibilityService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <provider
            android:name="com.safeNest.demo.features.urlGuard.impl.urlGuard.BlockedPageContentProvider"
            android:authorities="com.safenest.urlGuard.blocked"
            android:exported="true"
            android:grantUriPermissions="true" />

        <provider
            android:name="com.safeNest.demo.features.urlGuard.impl.urlGuard.FormWarningPageContentProvider"
            android:authorities="com.safenest.urlGuard.warning"
            android:exported="true"
            android:grantUriPermissions="true" />
    </application>

</manifest>